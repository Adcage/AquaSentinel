# STM32 收尾同步 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `firmware/stm32-ptz` 的电量测试、交接文档和当前代码状态重新一致，收尾掉已经完成但未同步的 STM32 工作。

**Architecture:** 不改动 STM32 现有运行行为，只做三类收尾：修正 `BatteryMonitor` 映射表对应的主机侧断言、更新交接文档中过时的“未提交”和“下一步”描述、最后重新跑固件构建和主机侧测试确认收尾结果。这样风险最小，也能把代码、测试、文档三条线重新对齐。

**Tech Stack:** PlatformIO / Arduino STM32 / C++11 / Markdown 文档

---

### Task 1: 同步电量映射测试预期

**Files:**
- Modify: `firmware/stm32-ptz/test/host/test_battery_monitor.cpp`
- Reference: `firmware/stm32-ptz/lib/battery_monitor/BatteryMonitor.cpp`

- [ ] **Step 1: 先把当前映射逻辑写成明确预期**

在 `firmware/stm32-ptz/test/host/test_battery_monitor.cpp` 中，把插值断言改成和 `BATTERY_PERCENT_POINTS` 真实线性插值一致：

```cpp
    assert(BatteryMonitor::batteryMvToPercent(3100) == 3);
    assert(BatteryMonitor::batteryMvToPercent(3500) == 22);
    assert(BatteryMonitor::batteryMvToPercent(3850) == 70);
```

修改前问题：`3100` 和 `3500` 的断言还停留在旧预期，分别写成了 `2` 和 `25`，和当前映射表不一致。

- [ ] **Step 2: 运行单测确认旧断言确实失败**

Run:

```bash
g++ -std=gnu++11 -Iinclude -Ilib/battery_monitor test/host/test_battery_monitor.cpp lib/battery_monitor/BatteryMonitor.cpp -o /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_battery && /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_battery
```

Expected: 在修改前于 `assert(BatteryMonitor::batteryMvToPercent(3100) == 2)` 处失败。

- [ ] **Step 3: 应用最小测试修正**

只改断言，不改 `BatteryMonitor.cpp` 逻辑，确保此次收尾不引入行为变化。

- [ ] **Step 4: 重新运行电量测试确认通过**

Run:

```bash
g++ -std=gnu++11 -Iinclude -Ilib/battery_monitor test/host/test_battery_monitor.cpp lib/battery_monitor/BatteryMonitor.cpp -o /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_battery && /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_battery
```

Expected: 退出码为 `0`，无断言失败输出。

### Task 2: 更新 STM32 交接文档状态

**Files:**
- Modify: `docs/archive/summaries/stm32-ptz-handoff-status.md`
- Reference: `docs/archive/plans/stm32-pb12-oled-interaction-plan.md`
- Reference: `firmware/stm32-ptz/src/main.cpp`
- Reference: `firmware/stm32-ptz/lib/button/ButtonHandler.cpp`

- [ ] **Step 1: 删除过时的“当前未提交文件”描述**

把 `docs/archive/summaries/stm32-ptz-handoff-status.md` 第 8 节从“当前未提交文件”改成“当前仓库状态”，明确说明本轮 ADC 采样和映射表改动已经提交到仓库，不再保留旧的未提交清单。

建议替换为：

```md
## 8. 当前仓库状态

截至本次交接检查，文档中提到的 ADC 采样修复与电量百分比映射调整代码已经提交到仓库，`BatteryMonitor.cpp` 与 `test_battery_monitor.cpp` 不再处于“未提交改动”状态。
```

- [ ] **Step 2: 收敛“下一步”里的本地校准规划**

把第 9.2 节从“后续功能”改成更准确的“暂缓项与后续建议”，明确说明：

```md
### 9.2 暂缓项与后续建议

- `PB12` 超长按进入校准模式：当前暂不实现。现有单键更适合承担切页和回中，本地校准若无新增输入会导致交互复杂且误触风险高。
- 本地校准入口联动：当前暂不实现，继续以 Web/串口校准链路为主；若后续确有脱离 Web 的需求，建议先增加新的本地输入按键后再单独设计交互。
- 硬件分压排查：若后续仍需继续追求电量精度，可继续定位 `BAT_SENSE` 中点被额外下拉的硬件根因，并评估是否将 `100k/100k` 分压改为 `10k/10k`。
```

修改前问题：文档仍把“本地校准入口联动”写成自然下一步，但当前产品决策已经收敛为暂不做。

- [ ] **Step 3: 补一行测试状态说明**

在文档第 7 节或第 8 节末尾补一句，说明主机侧 `test_battery_monitor.cpp` 已与最新映射表同步，避免后续读文档的人误以为当前仓库仍存在测试漂移。

建议文案：

```md
- 本次收尾已同步修正 `test_battery_monitor.cpp` 中与新映射表不一致的插值断言，当前测试预期与代码实现一致。
```

### Task 3: 重新验证 STM32 收尾结果

**Files:**
- Verify: `firmware/stm32-ptz/platformio.ini`
- Verify: `firmware/stm32-ptz/test/host/test_battery_monitor.cpp`
- Verify: `firmware/stm32-ptz/test/host/test_oled_view_model_pages.cpp`
- Verify: `firmware/stm32-ptz/test/host/test_button_handler.cpp`

- [ ] **Step 1: 重新构建固件**

Run:

```bash
pio run -e bluepill_f103c8
```

Expected: `SUCCESS`，并输出 RAM/Flash 占用。

- [ ] **Step 2: 运行电量监控主机测试**

Run:

```bash
g++ -std=gnu++11 -Iinclude -Ilib/battery_monitor test/host/test_battery_monitor.cpp lib/battery_monitor/BatteryMonitor.cpp -o /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_battery && /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_battery
```

Expected: 退出码 `0`。

- [ ] **Step 3: 运行 OLED 页面主机测试**

Run:

```bash
g++ -std=gnu++11 -Iinclude -Ilib/oled_display test/host/test_oled_view_model_pages.cpp lib/oled_display/OledViewModel.cpp -o /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_oled && /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_oled
```

Expected: 退出码 `0`。

- [ ] **Step 4: 运行按键处理主机测试**

Run:

```bash
g++ -std=gnu++11 -Iinclude -Ilib/button test/host/test_button_handler.cpp lib/button/ButtonHandler.cpp -o /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_button && /var/folders/cc/vmnhxdy55k79lk4ylmtsz26m0000gn/T/opencode/stm32_test_button
```

Expected: 退出码 `0`。

---

## 自检结论

- 计划覆盖了这次 STM32 收尾的全部目标：测试同步、文档同步、验证同步。
- 计划没有引入新的功能范围，只针对现有代码和文档的状态漂移做修正，符合最小改动原则。
- 所有步骤都给出了明确文件和验证命令，没有保留占位项。
